version: 2

models:
  - name: stg_stripe
    description: One payment per row
    columns:
      - name: payment_id
        description: Primary key for payments
        tests:
          - unique
          - not_null